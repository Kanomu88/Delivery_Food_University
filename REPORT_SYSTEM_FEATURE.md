# ระบบคำขอรายงานและการสร้างรายงาน

## ภาพรวม
ระบบนี้เพิ่มฟีเจอร์ใหม่ที่ช่วยให้ร้านค้าสามารถขอรายงานการขายจากแอดมิน และแอดมินสามารถสร้าง แก้ไข และส่งออกรายงานเป็น PDF หรือ Text ได้

## ฟีเจอร์หลัก

### 1. สำหรับร้านค้า (Vendor)
- **ปุ่มขอรายงาน**: ในหน้า `/vendor/reports` มีปุ่ม "📊 ขอรายงานจากแอดมิน"
- เมื่อกดปุ่ม คำขอจะถูกส่งไปยังแอดมินทุกคน
- แอดมินจะได้รับการแจ้งเตือนผ่านระบบ notification

### 2. สำหรับแอดมิน (Admin)
- **หน้าจัดการรายงาน**: `/admin/reports`
- ดูคำขอรายงานทั้งหมดจากร้านค้า
- กรองตามสถานะ: ทั้งหมด, รอดำเนินการ, เสร็จสิ้น

#### การสร้างรายงาน
1. คลิกปุ่ม "📝 สร้างรายงาน" ในคำขอที่รอดำเนินการ
2. เลือกร้านค้า (ถ้ายังไม่ได้เลือก)
3. เลือกช่วงวันที่ที่ต้องการสร้างรายงาน
4. คลิก "🔍 สร้างรายงาน"
5. ระบบจะแสดงข้อมูล:
   - ข้อมูลร้านค้า
   - ช่วงเวลา
   - สรุปยอดขาย (รายได้รวม, จำนวนออเดอร์, ค่าเฉลี่ยต่อออเดอร์)
   - เมนูขายดี Top 10

#### การแก้ไขรายงาน
1. คลิกปุ่ม "✏️ แก้ไข/ดูรายงาน" ในรายงานที่เสร็จสิ้นแล้ว
2. คลิกปุ่ม "✏️ แก้ไข" เพื่อเข้าสู่โหมดแก้ไข
3. สามารถแก้ไข:
   - รายได้รวม
   - จำนวนออเดอร์
   - ชื่อเมนู, จำนวน, รายได้ของแต่ละเมนู
   - เพิ่มหรือลบเมนู
4. คลิก "💾 บันทึก" เพื่อบันทึกการแก้ไข

#### การส่งออกรายงาน
- **PDF**: คลิกปุ่ม "📄 ส่งออก PDF" เพื่อดาวน์โหลดรายงานเป็น PDF
- **Text**: คลิกปุ่ม "📝 ส่งออก Text" เพื่อดาวน์โหลดรายงานเป็นไฟล์ข้อความ

## โครงสร้างไฟล์ที่เพิ่มใหม่

### Backend
```
backend/
├── models/
│   └── ReportRequest.js          # Model สำหรับคำขอรายงาน
├── controllers/
│   └── reportController.js       # Controller จัดการรายงาน
└── routes/
    └── reportRoutes.js           # Routes สำหรับ API รายงาน
```

### Frontend
```
frontend/
├── src/
│   ├── pages/
│   │   ├── AdminReportsPage.jsx       # หน้าจัดการรายงานของแอดมิน
│   │   └── AdminReportsPage.css
│   └── components/
│       └── admin/
│           ├── ReportGeneratorModal.jsx    # Modal สร้างรายงาน
│           ├── ReportGeneratorModal.css
│           ├── ReportEditorModal.jsx       # Modal แก้ไขรายงาน
│           └── ReportEditorModal.css
```

## API Endpoints

### Vendor Endpoints
- `POST /api/reports/request` - ส่งคำขอรายงาน

### Admin Endpoints
- `GET /api/reports/requests` - ดูคำขอรายงานทั้งหมด
- `GET /api/reports/requests/:requestId` - ดูรายละเอียดคำขอ
- `POST /api/reports/generate/:requestId` - สร้างรายงาน
- `PUT /api/reports/update/:requestId` - อัปเดตรายงาน
- `GET /api/reports/vendors` - ดูรายชื่อร้านค้าทั้งหมด

## Database Schema

### ReportRequest Model
```javascript
{
  vendorId: ObjectId,           // อ้างอิงถึงร้านค้า
  requestedBy: ObjectId,        // ผู้ขอรายงาน
  status: String,               // pending, processing, completed, rejected
  reportData: Mixed,            // ข้อมูลรายงาน
  processedBy: ObjectId,        // แอดมินที่ดำเนินการ
  processedAt: Date,            // วันที่ดำเนินการ
  notes: String,                // หมายเหตุ
  createdAt: Date,
  updatedAt: Date
}
```

## การแจ้งเตือน

### Notification Types
- `report_request` - แจ้งเตือนแอดมินเมื่อมีคำขอรายงานใหม่
- `system` - แจ้งเตือนร้านค้าเมื่อรายงานพร้อมแล้ว

## Dependencies ที่เพิ่มใหม่
- `jspdf` - สำหรับสร้างไฟล์ PDF

## การติดตั้ง

1. ติดตั้ง dependencies:
```bash
cd frontend
npm install jspdf
```

2. รัน backend server:
```bash
cd backend
npm run dev
```

3. รัน frontend:
```bash
cd frontend
npm run dev
```

## การใช้งาน

### สำหรับร้านค้า
1. เข้าสู่ระบบด้วยบัญชีร้านค้า
2. ไปที่หน้า "รายงานยอดขาย" (`/vendor/reports`)
3. คลิกปุ่ม "📊 ขอรายงานจากแอดมิน"
4. รอแอดมินดำเนินการสร้างรายงาน
5. จะได้รับการแจ้งเตือนเมื่อรายงานพร้อม

### สำหรับแอดมิน
1. เข้าสู่ระบบด้วยบัญชีแอดมิน
2. ไปที่หน้า "จัดการรายงาน" (`/admin/reports`)
3. ดูคำขอรายงานที่รอดำเนินการ
4. คลิก "📝 สร้างรายงาน" เพื่อสร้างรายงาน
5. เลือกร้านค้าและช่วงวันที่
6. แก้ไขรายงานตามต้องการ
7. ส่งออกเป็น PDF หรือ Text

## หมายเหตุ
- รายงาน PDF จะแสดงข้อมูลเป็นภาษาอังกฤษเนื่องจากข้อจำกัดของ jsPDF ในการรองรับฟอนต์ภาษาไทย
- สามารถส่งออกเป็นไฟล์ Text เพื่อรองรับภาษาไทยได้
- แอดมินสามารถแก้ไขรายงานได้หลายครั้งตามต้องการ
- ร้านค้าจะได้รับการแจ้งเตือนเมื่อรายงานพร้อมแล้ว

## การพัฒนาต่อ
- เพิ่มการส่งออกเป็น Excel
- เพิ่มกราฟและชาร์ตในรายงาน
- เพิ่มการส่งรายงานทางอีเมล
- เพิ่มการกำหนดเทมเพลตรายงาน
- เพิ่มการจัดกำหนดการสร้างรายงานอัตโนมัติ
