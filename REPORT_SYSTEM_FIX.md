# 🔧 การแก้ไขระบบรายงาน (Report System Fix)

## ปัญหาที่พบ

### 1. หน้า Vendor Reports (`/vendor/reports`)
- ปุ่ม "ส่งคำขอรายงาน" ไม่แสดงหรือไม่ชัดเจน

### 2. หน้า Admin Reports (`/admin/reports`)
- ไม่มีส่วนของการสร้างรายงานที่ร้านค้าขอมา

## การแก้ไข

### ✅ 1. ปรับปรุง CSS ของปุ่มส่งคำขอรายงาน (Vendor)

**ไฟล์:** `frontend/src/pages/VendorReportsPage.css`

```css
.request-report-btn {
  padding: 0.75rem 1.5rem;
  background: linear-gradient(135deg, #4CAF50, #66BB6A);
  color: white;
  border: none;
  border-radius: 8px;
  font-weight: 600;
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.3s;
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  white-space: nowrap;
}
```

**การเปลี่ยนแปลง:**
- เปลี่ยนสีปุ่มเป็นสีเขียวเพื่อให้เด่นชัดขึ้น
- เพิ่ม `display: inline-flex` เพื่อให้แสดงผลถูกต้อง
- เพิ่ม `white-space: nowrap` เพื่อไม่ให้ข้อความตัด

### ✅ 2. ระบบรายงานสำหรับ Admin

**ไฟล์ที่เกี่ยวข้อง:**
- `frontend/src/pages/AdminReportsPage.jsx` - หน้าหลักจัดการรายงาน
- `frontend/src/components/admin/ReportGeneratorModal.jsx` - Modal สร้างรายงาน
- `frontend/src/components/admin/ReportEditorModal.jsx` - Modal แก้ไข/ดูรายงาน

**ฟีเจอร์ที่มี:**

#### 📋 หน้าจัดการรายงาน (Admin Reports Page)
- แสดงรายการคำขอรายงานทั้งหมด
- กรองตามสถานะ: ทั้งหมด, รอดำเนินการ, เสร็จสิ้น
- แสดงข้อมูล:
  - ชื่อร้านค้า
  - ผู้ขอรายงาน
  - วันที่ขอ
  - สถานะ
  - ผู้ดำเนินการ (ถ้ามี)

#### 📝 Modal สร้างรายงาน (Report Generator)
- เลือกร้านค้า
- เลือกช่วงวันที่
- สร้างรายงานอัตโนมัติจากข้อมูลออเดอร์
- แสดงข้อมูล:
  - รายได้รวม
  - จำนวนออเดอร์
  - ค่าเฉลี่ยต่อออเดอร์
  - เมนูขายดี Top 10

#### ✏️ Modal แก้ไข/ดูรายงาน (Report Editor)
- ดูรายงานที่สร้างแล้ว
- แก้ไขข้อมูลรายงาน
- เพิ่ม/ลบเมนู
- ส่งออกเป็น PDF
- ส่งออกเป็น Text file

### ✅ 3. Backend API

**ไฟล์:** `backend/routes/reportRoutes.js`

**Endpoints:**

#### Vendor Routes
- `POST /api/reports/request` - ส่งคำขอรายงาน

#### Admin Routes
- `GET /api/reports/requests` - ดูรายการคำขอทั้งหมด
- `GET /api/reports/requests/:requestId` - ดูรายละเอียดคำขอ
- `POST /api/reports/generate/:requestId` - สร้างรายงาน
- `PUT /api/reports/update/:requestId` - แก้ไขรายงาน
- `GET /api/reports/vendors` - ดูรายชื่อร้านค้า

### ✅ 4. Database Model

**ไฟล์:** `backend/models/ReportRequest.js`

**Schema:**
```javascript
{
  vendorId: ObjectId,
  requestedBy: ObjectId,
  status: 'pending' | 'processing' | 'completed' | 'rejected',
  reportData: {
    vendor: { id, name, location },
    period: { startDate, endDate },
    summary: { totalRevenue, totalOrders, averageOrderValue },
    popularMenus: [{ name, quantity, revenue }],
    generatedAt: Date,
    generatedBy: ObjectId
  },
  processedBy: ObjectId,
  processedAt: Date,
  createdAt: Date,
  updatedAt: Date
}
```

## การใช้งาน

### สำหรับร้านค้า (Vendor)

1. เข้าหน้า Reports (`/vendor/reports`)
2. คลิกปุ่ม "📊 ขอรายงานจากแอดมิน" (สีเขียว ด้านบนขวา)
3. ระบบจะส่งคำขอไปยังแอดมิน
4. รอแอดมินสร้างรายงานให้

### สำหรับแอดมิน (Admin)

1. เข้าหน้า Reports (`/admin/reports`)
2. ดูรายการคำขอรายงาน
3. คลิก "📝 สร้างรายงาน" สำหรับคำขอที่รอดำเนินการ
4. เลือกร้านค้าและช่วงวันที่
5. คลิก "🔍 สร้างรายงาน"
6. ตรวจสอบข้อมูลและคลิก "✅ บันทึกและปิด"
7. สามารถแก้ไขรายงานได้โดยคลิก "✏️ แก้ไข/ดูรายงาน"
8. ส่งออกเป็น PDF หรือ Text file ได้

## การแจ้งเตือน

### เมื่อร้านค้าขอรายงาน
- แอดมินทุกคนจะได้รับการแจ้งเตือน

### เมื่อแอดมินสร้างรายงานเสร็จ
- ร้านค้าจะได้รับการแจ้งเตือน

## การทดสอบ

### ทดสอบด้วย Script
```bash
node scripts/testReportSystem.js
```

### ทดสอบด้วยตนเอง

#### 1. ทดสอบ Vendor
- Login: vendor1@test.com / password123
- ไปที่: https://university-canteen-ordering-system.vercel.app/vendor/reports
- คลิกปุ่ม "ขอรายงานจากแอดมิน"
- ตรวจสอบว่าได้รับ notification สำเร็จ

#### 2. ทดสอบ Admin
- Login: admin@test.com / admin123
- ไปที่: https://university-canteen-ordering-system.vercel.app/admin/reports
- ตรวจสอบว่ามีคำขอรายงานแสดง
- คลิก "สร้างรายงาน"
- เลือกร้านค้าและวันที่
- สร้างรายงาน
- ทดสอบแก้ไขรายงาน
- ทดสอบส่งออก PDF และ Text

## สถานะการแก้ไข

✅ ปรับปรุง CSS ปุ่มส่งคำขอรายงาน (Vendor)
✅ ระบบจัดการรายงานสำหรับ Admin
✅ Modal สร้างรายงาน
✅ Modal แก้ไข/ดูรายงาน
✅ Backend API endpoints
✅ Database model
✅ การแจ้งเตือน
✅ ส่งออก PDF/Text

## หมายเหตุ

- ปุ่ม "ขอรายงานจากแอดมิน" จะแสดงสีเขียวเพื่อให้เด่นชัด
- Admin สามารถสร้างรายงานได้หลายครั้งสำหรับคำขอเดียวกัน
- รายงานสามารถแก้ไขได้หลังจากสร้างแล้ว
- รองรับการส่งออกเป็น PDF และ Text file
- ระบบจะคำนวณข้อมูลจากออเดอร์ที่สถานะ "completed" เท่านั้น

## การ Deploy

ระบบพร้อมใช้งานแล้วบน Production:
- Frontend: https://university-canteen-ordering-system.vercel.app
- Backend: https://university-canteen-ordering-system.vercel.app/api

ไม่จำเป็นต้อง redeploy เพราะโค้ดทั้งหมดมีอยู่แล้ว เพียงแค่แก้ไข CSS เล็กน้อย
