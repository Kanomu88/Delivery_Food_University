# 🚀 Quick Start - ระบบรายงาน

## เริ่มต้นใช้งานภายใน 5 นาที

### 📦 ติดตั้ง Dependencies

```bash
# ติดตั้ง jsPDF สำหรับ frontend
cd frontend
npm install jspdf
```

### 🏃‍♂️ รันระบบ

```bash
# Terminal 1 - Backend
cd backend
npm run dev

# Terminal 2 - Frontend  
cd frontend
npm run dev
```

### 🎯 ทดสอบฟีเจอร์

#### สำหรับร้านค้า (Vendor)
1. เข้าสู่ระบบด้วยบัญชีร้านค้า
2. ไปที่ https://localhost:5173/vendor/reports
3. คลิกปุ่ม "📊 ขอรายงานจากแอดมิน"
4. ✅ เสร็จสิ้น! แอดมินจะได้รับการแจ้งเตือน

#### สำหรับแอดมิน (Admin)
1. เข้าสู่ระบบด้วยบัญชีแอดมิน
2. ไปที่ https://localhost:5173/admin/reports
3. คลิก "📝 สร้างรายงาน" ในคำขอที่รอดำเนินการ
4. เลือกร้านค้าและช่วงวันที่
5. คลิก "🔍 สร้างรายงาน"
6. ✅ เสร็จสิ้น! รายงานถูกสร้างแล้ว

#### แก้ไขและส่งออกรายงาน
1. คลิก "✏️ แก้ไข/ดูรายงาน"
2. คลิก "✏️ แก้ไข" เพื่อแก้ไขข้อมูล
3. คลิก "💾 บันทึก" เมื่อเสร็จ
4. คลิก "📄 ส่งออก PDF" หรือ "📝 ส่งออก Text"
5. ✅ เสร็จสิ้น! ไฟล์ถูกดาวน์โหลดแล้ว

## 📁 โครงสร้างไฟล์

```
project/
├── backend/
│   ├── models/
│   │   ├── ReportRequest.js          ← ใหม่
│   │   └── Notification.js           ← แก้ไข
│   ├── controllers/
│   │   └── reportController.js       ← ใหม่
│   ├── routes/
│   │   └── reportRoutes.js           ← ใหม่
│   └── server.js                     ← แก้ไข
│
└── frontend/
    └── src/
        ├── pages/
        │   ├── AdminReportsPage.jsx       ← ใหม่
        │   ├── AdminReportsPage.css       ← ใหม่
        │   ├── VendorReportsPage.jsx      ← แก้ไข
        │   └── VendorReportsPage.css      ← แก้ไข
        ├── components/
        │   └── admin/
        │       ├── ReportGeneratorModal.jsx    ← ใหม่
        │       ├── ReportGeneratorModal.css    ← ใหม่
        │       ├── ReportEditorModal.jsx       ← ใหม่
        │       └── ReportEditorModal.css       ← ใหม่
        ├── i18n/
        │   └── locales/
        │       └── th.json                ← แก้ไข
        └── App.jsx                        ← แก้ไข
```

## 🔑 API Endpoints

### Vendor
```
POST /api/reports/request
- ส่งคำขอรายงาน
- Auth: Bearer token (vendor)
```

### Admin
```
GET /api/reports/requests
- ดูคำขอทั้งหมด
- Auth: Bearer token (admin)

POST /api/reports/generate/:requestId
- สร้างรายงาน
- Auth: Bearer token (admin)
- Body: { vendorId, startDate, endDate }

PUT /api/reports/update/:requestId
- แก้ไขรายงาน
- Auth: Bearer token (admin)
- Body: { reportData }

GET /api/reports/vendors
- ดูรายชื่อร้านค้า
- Auth: Bearer token (admin)
```

## 💡 ตัวอย่างการใช้งาน

### 1. ร้านค้าขอรายงาน
```javascript
// Frontend - VendorReportsPage.jsx
const handleRequestReport = async () => {
  const response = await fetch(`${API_URL}/api/reports/request`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
    },
  });
  // แอดมินได้รับการแจ้งเตือน
};
```

### 2. แอดมินสร้างรายงาน
```javascript
// Frontend - ReportGeneratorModal.jsx
const handleGenerateReport = async () => {
  const response = await fetch(
    `${API_URL}/api/reports/generate/${requestId}`,
    {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${token}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        vendorId: selectedVendor,
        startDate: '2024-01-01',
        endDate: '2024-01-31',
      }),
    }
  );
  // รายงานถูกสร้างพร้อมข้อมูล
};
```

### 3. ส่งออก PDF
```javascript
// Frontend - ReportEditorModal.jsx
import jsPDF from 'jspdf';

const handleExportPDF = () => {
  const doc = new jsPDF();
  doc.text('Sales Report', 105, 20, { align: 'center' });
  doc.text(`Vendor: ${reportData.vendor.name}`, 20, 40);
  // ... เพิ่มข้อมูลอื่นๆ
  doc.save(`report-${vendorName}-${Date.now()}.pdf`);
};
```

## 🎨 UI Components

### AdminReportsPage
- แสดงรายการคำขอรายงาน
- กรองตามสถานะ
- ปุ่มสร้างรายงาน/แก้ไขรายงาน

### ReportGeneratorModal
- เลือกร้านค้า
- เลือกช่วงวันที่
- สร้างรายงานอัตโนมัติ
- แสดงผลรายงาน

### ReportEditorModal
- ดูรายงาน
- แก้ไขข้อมูล
- เพิ่ม/ลบเมนู
- ส่งออก PDF/Text

## 🔔 Notifications

### แอดมินได้รับการแจ้งเตือนเมื่อ:
- ร้านค้าส่งคำขอรายงานใหม่

### ร้านค้าได้รับการแจ้งเตือนเมื่อ:
- แอดมินสร้างรายงานเสร็จ

## 🐛 Troubleshooting

### ปัญหา: ไม่สามารถส่งคำขอรายงานได้
```bash
# ตรวจสอบว่ามี vendor profile
# ตรวจสอบ token ใน localStorage
# ตรวจสอบ console log
```

### ปัญหา: PDF ไม่แสดงภาษาไทย
```bash
# ใช้การส่งออกเป็น Text แทน
# หรือเพิ่มฟอนต์ภาษาไทยใน jsPDF
```

### ปัญหา: รายงานไม่มีข้อมูล
```bash
# เลือกช่วงวันที่ที่มีออเดอร์
# ตรวจสอบว่ามีออเดอร์ที่ status = 'completed'
```

## 📚 เอกสารเพิ่มเติม

- [REPORT_SYSTEM_FEATURE.md](./REPORT_SYSTEM_FEATURE.md) - คู่มือการใช้งานแบบละเอียด
- [TEST_REPORT_SYSTEM.md](./TEST_REPORT_SYSTEM.md) - คู่มือการทดสอบ
- [DEPLOY_REPORT_FEATURE.md](./DEPLOY_REPORT_FEATURE.md) - คู่มือการ deploy
- [REPORT_FEATURE_SUMMARY.md](./REPORT_FEATURE_SUMMARY.md) - สรุปการพัฒนา

## ✅ Checklist

### ก่อนใช้งาน
- [ ] ติดตั้ง jsPDF แล้ว
- [ ] Backend รันอยู่
- [ ] Frontend รันอยู่
- [ ] มีบัญชีร้านค้าและแอดมิน

### ทดสอบฟีเจอร์
- [ ] ร้านค้าส่งคำขอรายงานได้
- [ ] แอดมินได้รับการแจ้งเตือน
- [ ] แอดมินสร้างรายงานได้
- [ ] แอดมินแก้ไขรายงานได้
- [ ] ส่งออก PDF ได้
- [ ] ส่งออก Text ได้

## 🎉 เสร็จสิ้น!

ตอนนี้คุณพร้อมใช้งานระบบรายงานแล้ว! 

หากมีคำถามหรือพบปัญหา กรุณาดูเอกสารเพิ่มเติมหรือติดต่อทีมพัฒนา

---

**Happy Reporting! 📊**
