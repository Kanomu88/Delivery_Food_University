# 🔧 แก้ไข Error 404 - Backend ยังไม่มี Report API

## ปัญหา
```
POST https://university-canteen-backend.vercel.app/api/reports/request 404 (Not Found)
```

แสดงว่า backend ยังไม่มี report endpoints

## วิธีแก้ไข

### ขั้นตอนที่ 1: ไปที่ Backend Project
คุณต้องไปที่ backend project ของคุณ (ไม่ใช่ project นี้)

### ขั้นตอนที่ 2: คัดลอกโค้ด
เปิดไฟล์ `COPY_TO_BACKEND.txt` ในโปรเจคนี้
คัดลอกโค้ดทั้งหมด

### ขั้นตอนที่ 3: วางโค้ดใน Backend
ไปที่ backend project แล้วแก้ไขไฟล์:
- `api/index.js` หรือ
- `index.js`

วางโค้ดที่คัดลอกมา **ก่อน** บรรทัด `export default app;`

### ขั้นตอนที่ 4: Deploy Backend
```bash
# ใน backend project
git add .
git commit -m "Add report API endpoints"
git push origin main

# หรือ
vercel --prod
```

### ขั้นตอนที่ 5: รอ 1-2 นาที
ให้ Vercel deploy backend เสร็จ

### ขั้นตอนที่ 6: ทดสอบ
ลองเรียก API:
```bash
curl https://university-canteen-backend.vercel.app/
```

ควรได้:
```json
{
  "message": "University Canteen Backend API",
  "status": "running",
  "version": "1.0.0"
}
```

### ขั้นตอนที่ 7: ทดสอบ Frontend
ไปที่: https://university-canteen-ordering-system.vercel.app/vendor/reports
คลิกปุ่ม "ขอรายงานจากแอดมิน"

## หมายเหตุ

- **ต้องอัพเดต backend ก่อน** frontend ถึงจะทำงานได้
- ถ้าคุณไม่มี backend project ใน local บอกฉันมา ฉันจะช่วยหาทางอื่น
- ถ้าคุณไม่แน่ใจว่า backend อยู่ที่ไหน ไปดูที่:
  https://vercel.com/esp32s-projects/university-canteen-backend
  คลิก Settings → Git → จะเห็น Git Repository URL

## ถ้ายังไม่ได้

บอกฉันว่า:
1. คุณมี backend project ใน local ไหม?
2. หรือต้องการให้ฉันช่วยหาวิธีอื่น?

ฉันพร้อมช่วยทุกขั้นตอน! 🚀
