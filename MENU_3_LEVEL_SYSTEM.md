# 🏢 ระบบเมนู 3 ระดับ (Canteen → Vendor → Menu)

## ✨ ฟีเจอร์ใหม่

### ระบบ 3 ระดับ:
1. **ระดับ 1: โรงอาหาร (Canteen)**
   - แสดง Card โรงอาหารทั้งหมด
   - มีรูปภาพ, ชื่อ, คำอธิบาย, ที่ตั้ง

2. **ระดับ 2: ร้านอาหาร (Vendor)**
   - แสดงร้านอาหารในโรงอาหารที่เลือก
   - แสดงสถานะเปิด/ปิดรับคำสั่งซื้อ
   - มีรูปภาพ, ชื่อร้าน, คำอธิบาย

3. **ระดับ 3: เมนูอาหาร (Menu)**
   - แสดงเมนูอาหารของร้านที่เลือก
   - มี Checkbox คำขอพิเศษ 5 ตัวเลือก:
     - ไม่ใส่ผัก
     - ไม่เผ็ด
     - เผ็ดน้อย
     - เผ็ดมาก
     - ไม่ใส่ผงชูรส

### คำขอพิเศษ (Special Options):
- ✅ Checkbox สำหรับแต่ละเมนู
- ✅ เลือกได้หลายตัวเลือก
- ✅ บันทึกไปกับตะกร้าสินค้า
- ✅ แสดงใน Order Details

## 📁 ไฟล์ที่สร้างใหม่

### Backend:
1. **`backend/models/Canteen.js`**
   - Model สำหรับโรงอาหาร
   - Fields: name, nameEn, description, image, location, building, floor, isActive, order

2. **`backend/controllers/canteenController.js`**
   - getAllCanteens() - ดึงโรงอาหารทั้งหมด
   - getCanteenById() - ดึงโรงอาหารตาม ID
   - getVendorsByCanteen() - ดึงร้านอาหารในโรงอาหาร
   - createCanteen() - สร้างโรงอาหาร (Admin)
   - updateCanteen() - แก้ไขโรงอาหาร (Admin)
   - deleteCanteen() - ลบโรงอาหาร (Admin)

3. **`backend/routes/canteenRoutes.js`**
   - GET /canteens - ดึงโรงอาหารทั้งหมด
   - GET /canteens/:id - ดึงโรงอาหารตาม ID
   - GET /canteens/:id/vendors - ดึงร้านอาหารในโรงอาหาร
   - POST /canteens - สร้างโรงอาหาร (Admin)
   - PUT /canteens/:id - แก้ไขโรงอาหาร (Admin)
   - DELETE /canteens/:id - ลบโรงอาหาร (Admin)

4. **`backend/scripts/createSampleCanteens.js`**
   - Script สร้างข้อมูลโรงอาหารตัวอย่าง 4 แห่ง:
     - โรงอาหารกลาง
     - โรงอาหารคณะวิศวกรรมศาสตร์
     - โรงอาหารคณะแพทยศาสตร์
     - โรงอาหารหอพัก

### Frontend:
1. **`frontend/src/services/canteenService.js`**
   - getAllCanteens() - ดึงโรงอาหารทั้งหมด
   - getCanteenById() - ดึงโรงอาหารตาม ID
   - getVendorsByCanteen() - ดึงร้านอาหารในโรงอาหาร
   - getVendorMenus() - ดึงเมนูของร้าน

2. **`frontend/src/pages/NewMenuPage.jsx`**
   - Component หลักสำหรับระบบ 3 ระดับ
   - จัดการ state และ navigation
   - แสดง Checkbox คำขอพิเศษ

## 📝 ไฟล์ที่แก้ไข

### Backend:
1. **`backend/models/Vendor.js`**
   - เพิ่ม field `canteenId` (reference to Canteen)
   - เพิ่ม index สำหรับ canteenId

2. **`backend/api/index.js`**
   - เพิ่ม Canteen และ Vendor schemas
   - เพิ่ม routes สำหรับ canteens
   - เพิ่ม endpoint GET /api/canteens
   - เพิ่ม endpoint GET /api/canteens/:id
   - เพิ่ม endpoint GET /api/canteens/:id/vendors
   - เพิ่ม endpoint GET /api/vendors/:id/menus

### Frontend:
1. **`frontend/src/App.jsx`**
   - เปลี่ยน MenuPage เป็น NewMenuPage

2. **`frontend/src/pages/MenuPage.css`**
   - เพิ่ม styles สำหรับ back button
   - เพิ่ม styles สำหรับ subtitle, description, location
   - เพิ่ม styles สำหรับ vendor status
   - เพิ่ม styles สำหรับ special options checkboxes
   - เพิ่ม styles สำหรับ menu item card

## 🎯 วิธีการทำงาน

### User Flow:
```
1. เข้าหน้า /menu
   ↓
2. เลือกโรงอาหาร (Canteen)
   ↓
3. เลือกร้านอาหาร (Vendor)
   ↓
4. เลือกเมนูอาหาร (Menu)
   ↓
5. เลือกคำขอพิเศษ (Checkboxes)
   ↓
6. เพิ่มลงตะกร้า
```

### Data Flow:
```
Frontend                    Backend
--------                    -------
NewMenuPage
  ↓
canteenService.getAllCanteens()
  → GET /api/canteens
    ← Canteen.find()
  
เลือก Canteen
  ↓
canteenService.getVendorsByCanteen(canteenId)
  → GET /api/canteens/:id/vendors
    ← Vendor.find({ canteenId })
  
เลือก Vendor
  ↓
canteenService.getVendorMenus(vendorId)
  → GET /api/vendors/:id/menus
    ← Menu.find({ vendor: vendorId })
  
เลือก Menu + Options
  ↓
addToCart({ ...item, specialRequests })
```

## 🚀 การติดตั้งและใช้งาน

### 1. สร้างข้อมูลโรงอาหารตัวอย่าง:
```bash
cd backend
node scripts/createSampleCanteens.js
```

### 2. ตรวจสอบข้อมูล:
- โรงอาหารจะถูกสร้างขึ้น 4 แห่ง
- Vendors ที่มีอยู่จะถูก assign ให้กับโรงอาหารอัตโนมัติ

### 3. ทดสอบ API:
```bash
# ดึงโรงอาหารทั้งหมด
curl http://localhost:5000/api/canteens

# ดึงร้านอาหารในโรงอาหาร
curl http://localhost:5000/api/canteens/{canteenId}/vendors

# ดึงเมนูของร้าน
curl http://localhost:5000/api/vendors/{vendorId}/menus
```

### 4. ทดสอบ Frontend:
1. เข้า http://localhost:5173/menu
2. เลือกโรงอาหาร
3. เลือกร้านอาหาร
4. เลือกเมนูและคำขอพิเศษ
5. เพิ่มลงตะกร้า

## 📊 Database Schema

### Canteen Collection:
```javascript
{
  _id: ObjectId,
  name: String,           // "โรงอาหารกลาง"
  nameEn: String,         // "Central Canteen"
  description: String,
  descriptionEn: String,
  image: String,          // URL
  location: String,       // "อาคารกลาง ชั้น 1"
  building: String,       // "อาคารกลาง"
  floor: String,          // "1"
  isActive: Boolean,
  order: Number,          // สำหรับเรียงลำดับ
  createdAt: Date,
  updatedAt: Date
}
```

### Vendor Collection (Updated):
```javascript
{
  _id: ObjectId,
  userId: ObjectId,       // ref: User
  canteenId: ObjectId,    // ref: Canteen (NEW)
  shopName: String,
  description: String,
  logo: String,
  status: String,
  isAcceptingOrders: Boolean,
  createdAt: Date,
  updatedAt: Date
}
```

### MenuItem Collection (Unchanged):
```javascript
{
  _id: ObjectId,
  vendorId: ObjectId,     // ref: Vendor
  name: String,
  description: String,
  price: Number,
  image: String,
  category: String,
  allergenInfo: String,
  isAvailable: Boolean,
  createdAt: Date,
  updatedAt: Date
}
```

## 🎨 UI/UX Features

### Navigation:
- ✅ ปุ่ม "ย้อนกลับ" ทุกระดับ
- ✅ Breadcrumb แสดงตำแหน่งปัจจุบัน
- ✅ Smooth transitions

### Cards:
- ✅ Hover effects
- ✅ Image lazy loading
- ✅ Responsive grid layout

### Checkboxes:
- ✅ Custom styling
- ✅ Hover effects
- ✅ Selected state indication
- ✅ Multiple selection support

### Status Indicators:
- 🟢 เปิดรับคำสั่งซื้อ (สีเขียว)
- 🔴 ปิดรับคำสั่งซื้อ (สีแดง)

## 📱 Responsive Design

- Desktop: Grid 3-4 columns
- Tablet: Grid 2-3 columns
- Mobile: Grid 1 column

## ✅ Testing Checklist

### Backend:
- [ ] สร้างโรงอาหารได้
- [ ] ดึงโรงอาหารทั้งหมดได้
- [ ] ดึงร้านอาหารตามโรงอาหารได้
- [ ] ดึงเมนูตามร้านอาหารได้
- [ ] Vendor มี canteenId

### Frontend:
- [ ] แสดงโรงอาหารทั้งหมด
- [ ] คลิกเข้าโรงอาหารได้
- [ ] แสดงร้านอาหารในโรงอาหาร
- [ ] คลิกเข้าร้านอาหารได้
- [ ] แสดงเมนูของร้าน
- [ ] Checkbox คำขอพิเศษทำงาน
- [ ] เลือกหลาย checkbox ได้
- [ ] เพิ่มลงตะกร้าพร้อมคำขอพิเศษ
- [ ] ปุ่มย้อนกลับทำงาน
- [ ] Responsive ทุก screen size

## 🔧 API Endpoints

### Public Endpoints:
```
GET    /api/canteens                    - ดึงโรงอาหารทั้งหมด
GET    /api/canteens/:id                - ดึงโรงอาหารตาม ID
GET    /api/canteens/:id/vendors        - ดึงร้านอาหารในโรงอาหาร
GET    /api/vendors/:id/menus           - ดึงเมนูของร้าน
```

### Admin Endpoints:
```
POST   /api/canteens                    - สร้างโรงอาหาร
PUT    /api/canteens/:id                - แก้ไขโรงอาหาร
DELETE /api/canteens/:id                - ลบโรงอาหาร
```

## 💡 ตัวอย่างข้อมูล

### โรงอาหารตัวอย่าง:
1. โรงอาหารกลาง (Central Canteen)
2. โรงอาหารคณะวิศวกรรมศาสตร์ (Engineering Canteen)
3. โรงอาหารคณะแพทยศาสตร์ (Medical Canteen)
4. โรงอาหารหอพัก (Dormitory Canteen)

### คำขอพิเศษ:
- ไม่ใส่ผัก
- ไม่เผ็ด
- เผ็ดน้อย
- เผ็ดมาก
- ไม่ใส่ผงชูรส

## 🎯 ประโยชน์

1. **จัดระเบียบดีขึ้น**: แยกโรงอาหารและร้านอาหารชัดเจน
2. **ง่ายต่อการค้นหา**: ลูกค้าหาร้านที่ต้องการได้เร็วขึ้น
3. **ปรับแต่งได้**: เลือกคำขอพิเศษตามต้องการ
4. **ขยายได้**: เพิ่มโรงอาหารและร้านใหม่ได้ง่าย
5. **UX ดีขึ้น**: Navigation ชัดเจน มี breadcrumb

---

**สร้างเมื่อ:** 15 พฤศจิกายน 2567
**Version:** 1.0.0
